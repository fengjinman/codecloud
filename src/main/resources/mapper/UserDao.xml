<?xml version="1.0" encoding="UTF-8" ?>   
<!DOCTYPE mapper   
PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN"  
"http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">
<mapper namespace="com.example.demo.dao.UserDao">
	<select id="queryAll" resultType="com.example.demo.entity.User">
		select * from User
	</select>
	<select id="queryUser" resultType="com.example.demo.entity.User">
		select * from user where username = #{username}
	</select>
	<insert id="insertUser" >
		insert into user(username,password) values(#{username},#{password})
	</insert>
	<!--测试-->
	<select id="selectByOddUserId" resultType="com.example.demo.entity.User">
		select * from user where id = #{id}
	</select>
	<select id="selectByEvenUserId" resultType="com.example.demo.entity.User">
		select * from user where id = #{id}
	</select>
	<select id="queryUserById" resultType="com.example.demo.entity.User">
		select * from user where id = #{id}
	</select>
</mapper>